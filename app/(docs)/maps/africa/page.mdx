import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Africa Map',
  description: 'Interactive SVG map of Africa with 55 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Africa Map"
  description="Interactive SVG map of Africa with 55 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="africa"
>

<ExampleLoader
  filePath='app/(docs)/maps/africa/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
