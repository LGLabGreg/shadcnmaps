import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Argentina Map',
  description: 'Interactive SVG map of Argentina with 24 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Argentina Map"
  description="Interactive SVG map of Argentina with 24 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="argentina"
>

<ExampleLoader
  filePath='app/(docs)/maps/argentina/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
