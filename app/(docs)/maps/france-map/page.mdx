import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'France Map',
  description: 'Interactive SVG map of France with 13 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="France Map"
  description="Interactive SVG map of France with 13 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="france-map"
>

<ExampleLoader
  filePath='app/(docs)/maps/france-map/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
