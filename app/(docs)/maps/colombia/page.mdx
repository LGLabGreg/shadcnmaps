import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Colombia Map',
  description: 'Interactive SVG map of Colombia with 33 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Colombia Map"
  description="Interactive SVG map of Colombia with 33 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="colombia"
>

<ExampleLoader
  filePath='app/(docs)/maps/colombia/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
