import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Syria Map',
  description: 'Interactive SVG map of Syria with 14 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Syria Map"
  description="Interactive SVG map of Syria with 14 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="syria"
>

<ExampleLoader
  filePath='app/(docs)/maps/syria/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
