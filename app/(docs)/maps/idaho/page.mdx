import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Idaho',
  description: 'Interactive SVG map of Idaho with 44 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Idaho"
  description="Interactive SVG map of Idaho with 44 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="idaho"
>

<ExampleLoader
  filePath='app/(docs)/maps/idaho/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
