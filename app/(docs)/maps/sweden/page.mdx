import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Sweden Map',
  description: 'Interactive SVG map of Sweden with 21 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Sweden Map"
  description="Interactive SVG map of Sweden with 21 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="sweden"
>

<ExampleLoader
  filePath='app/(docs)/maps/sweden/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
