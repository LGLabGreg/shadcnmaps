import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Uruguay Map',
  description: 'Interactive SVG map of Uruguay with 19 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Uruguay Map"
  description="Interactive SVG map of Uruguay with 19 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="uruguay"
>

<ExampleLoader
  filePath='app/(docs)/maps/uruguay/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
