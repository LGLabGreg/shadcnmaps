import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Ecuador Map',
  description: 'Interactive SVG map of Ecuador with 24 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Ecuador Map"
  description="Interactive SVG map of Ecuador with 24 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="ecuador"
>

<ExampleLoader
  filePath='app/(docs)/maps/ecuador/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
