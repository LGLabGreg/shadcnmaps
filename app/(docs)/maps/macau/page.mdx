import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Macau Map',
  description: 'Interactive SVG map of Macau with 8 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Macau Map"
  description="Interactive SVG map of Macau with 8 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="macau"
>

<ExampleLoader
  filePath='app/(docs)/maps/macau/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
