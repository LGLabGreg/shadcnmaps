import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Connecticut',
  description: 'Interactive SVG map of Connecticut with 8 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Connecticut"
  description="Interactive SVG map of Connecticut with 8 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="connecticut"
>

<ExampleLoader
  filePath='app/(docs)/maps/connecticut/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
