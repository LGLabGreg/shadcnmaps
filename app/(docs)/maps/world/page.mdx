import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'World Map',
  description: 'Interactive SVG map of World with 256 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="World Map"
  description="Interactive SVG map of World with 256 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="world"
>

<ExampleLoader
  filePath='app/(docs)/maps/world/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
