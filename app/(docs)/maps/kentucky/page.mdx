import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Kentucky',
  description: 'Interactive SVG map of Kentucky with 120 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Kentucky"
  description="Interactive SVG map of Kentucky with 120 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="kentucky"
>

<ExampleLoader
  filePath='app/(docs)/maps/kentucky/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
