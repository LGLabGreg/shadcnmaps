import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Finland Map',
  description: 'Interactive SVG map of Finland with 18 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Finland Map"
  description="Interactive SVG map of Finland with 18 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="finland"
>

<ExampleLoader
  filePath='app/(docs)/maps/finland/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
