import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Croatia',
  description: 'Interactive SVG map of Croatia with 21 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Croatia"
  description="Interactive SVG map of Croatia with 21 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="croatia"
>

<ExampleLoader
  filePath='app/(docs)/maps/croatia/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
