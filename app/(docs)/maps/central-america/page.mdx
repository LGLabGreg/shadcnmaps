import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Central America',
  description: 'Interactive SVG map of Central America with 7 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Central America"
  description="Interactive SVG map of Central America with 7 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="central-america"
>

<ExampleLoader
  filePath='app/(docs)/maps/central-america/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
