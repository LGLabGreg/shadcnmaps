import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Belgium',
  description: 'Interactive SVG map of Belgium with 11 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Belgium"
  description="Interactive SVG map of Belgium with 11 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="belgium"
>

<ExampleLoader
  filePath='app/(docs)/maps/belgium/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
