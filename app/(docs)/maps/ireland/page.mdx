import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Ireland Map',
  description: 'Interactive SVG map of Ireland with 34 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Ireland Map"
  description="Interactive SVG map of Ireland with 34 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="ireland"
>

<ExampleLoader
  filePath='app/(docs)/maps/ireland/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
