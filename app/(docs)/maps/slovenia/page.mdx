import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Slovenia Map',
  description: 'Interactive SVG map of Slovenia with 193 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Slovenia Map"
  description="Interactive SVG map of Slovenia with 193 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="slovenia"
>

<ExampleLoader
  filePath='app/(docs)/maps/slovenia/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
