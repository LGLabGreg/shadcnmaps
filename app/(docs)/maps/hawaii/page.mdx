import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Hawaii',
  description: 'Interactive SVG map of Hawaii with 5 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Hawaii"
  description="Interactive SVG map of Hawaii with 5 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="hawaii"
>

<ExampleLoader
  filePath='app/(docs)/maps/hawaii/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
