import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Romania Map',
  description: 'Interactive SVG map of Romania with 42 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Romania Map"
  description="Interactive SVG map of Romania with 42 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="romania"
>

<ExampleLoader
  filePath='app/(docs)/maps/romania/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
