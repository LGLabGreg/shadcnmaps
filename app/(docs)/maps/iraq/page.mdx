import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Iraq',
  description: 'Interactive SVG map of Iraq with 18 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Iraq"
  description="Interactive SVG map of Iraq with 18 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="iraq"
>

<ExampleLoader
  filePath='app/(docs)/maps/iraq/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
