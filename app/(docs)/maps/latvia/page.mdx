import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Latvia Map',
  description: 'Interactive SVG map of Latvia with 119 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Latvia Map"
  description="Interactive SVG map of Latvia with 119 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="latvia"
>

<ExampleLoader
  filePath='app/(docs)/maps/latvia/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
