import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Cuba Map',
  description: 'Interactive SVG map of Cuba with 16 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Cuba Map"
  description="Interactive SVG map of Cuba with 16 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="cuba"
>

<ExampleLoader
  filePath='app/(docs)/maps/cuba/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
