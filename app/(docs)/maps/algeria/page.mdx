import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Algeria Map',
  description: 'Interactive SVG map of Algeria with 48 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Algeria Map"
  description="Interactive SVG map of Algeria with 48 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="algeria"
>

<ExampleLoader
  filePath='app/(docs)/maps/algeria/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
