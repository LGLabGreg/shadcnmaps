import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'United Kingdom',
  description: 'Interactive SVG map of United Kingdom with 14 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="United Kingdom"
  description="Interactive SVG map of United Kingdom with 14 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="uk"
>

<ExampleLoader
  filePath='app/(docs)/maps/uk/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
