import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Nepal Map',
  description: 'Interactive SVG map of Nepal with 14 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Nepal Map"
  description="Interactive SVG map of Nepal with 14 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="nepal"
>

<ExampleLoader
  filePath='app/(docs)/maps/nepal/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
