import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Estonia',
  description: 'Interactive SVG map of Estonia with 15 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Estonia"
  description="Interactive SVG map of Estonia with 15 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="estonia"
>

<ExampleLoader
  filePath='app/(docs)/maps/estonia/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
