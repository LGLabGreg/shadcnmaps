import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Egypt',
  description: 'Interactive SVG map of Egypt with 27 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Egypt"
  description="Interactive SVG map of Egypt with 27 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="egypt"
>

<ExampleLoader
  filePath='app/(docs)/maps/egypt/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
