import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'India',
  description: 'Interactive SVG map of India with 36 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="India"
  description="Interactive SVG map of India with 36 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="india"
>

<ExampleLoader
  filePath='app/(docs)/maps/india/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
