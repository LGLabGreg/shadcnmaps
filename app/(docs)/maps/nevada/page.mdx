import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Nevada',
  description: 'Interactive SVG map of Nevada with 17 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Nevada"
  description="Interactive SVG map of Nevada with 17 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="nevada"
>

<ExampleLoader
  filePath='app/(docs)/maps/nevada/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
