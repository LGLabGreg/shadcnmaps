import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Greece Map',
  description: 'Interactive SVG map of Greece with 14 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Greece Map"
  description="Interactive SVG map of Greece with 14 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="greece"
>

<ExampleLoader
  filePath='app/(docs)/maps/greece/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
