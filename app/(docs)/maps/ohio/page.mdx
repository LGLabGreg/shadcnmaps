import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Ohio',
  description: 'Interactive SVG map of Ohio with 88 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Ohio"
  description="Interactive SVG map of Ohio with 88 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="ohio"
>

<ExampleLoader
  filePath='app/(docs)/maps/ohio/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
