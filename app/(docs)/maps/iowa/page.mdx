import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Iowa',
  description: 'Interactive SVG map of Iowa with 99 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Iowa"
  description="Interactive SVG map of Iowa with 99 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="iowa"
>

<ExampleLoader
  filePath='app/(docs)/maps/iowa/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
