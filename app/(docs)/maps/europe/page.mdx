import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Europe',
  description: 'Interactive SVG map of Europe with 62 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Europe"
  description="Interactive SVG map of Europe with 62 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="europe"
>

<ExampleLoader
  filePath='app/(docs)/maps/europe/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
