import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Massachusetts Map',
  description: 'Interactive SVG map of Massachusetts with 14 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Massachusetts Map"
  description="Interactive SVG map of Massachusetts with 14 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="massachusetts"
>

<ExampleLoader
  filePath='app/(docs)/maps/massachusetts/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
