import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Zambia Map',
  description: 'Interactive SVG map of Zambia with 10 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Zambia Map"
  description="Interactive SVG map of Zambia with 10 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="zambia"
>

<ExampleLoader
  filePath='app/(docs)/maps/zambia/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
