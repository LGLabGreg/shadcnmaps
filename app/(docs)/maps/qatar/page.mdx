import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Qatar',
  description: 'Interactive SVG map of Qatar with 7 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Qatar"
  description="Interactive SVG map of Qatar with 7 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="qatar"
>

<ExampleLoader
  filePath='app/(docs)/maps/qatar/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
