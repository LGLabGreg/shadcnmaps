import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Monaco',
  description: 'Interactive SVG map of Monaco with 10 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Monaco"
  description="Interactive SVG map of Monaco with 10 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="monaco"
>

<ExampleLoader
  filePath='app/(docs)/maps/monaco/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
