import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Minnesota Map',
  description: 'Interactive SVG map of Minnesota with 87 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Minnesota Map"
  description="Interactive SVG map of Minnesota with 87 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="minnesota"
>

<ExampleLoader
  filePath='app/(docs)/maps/minnesota/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
