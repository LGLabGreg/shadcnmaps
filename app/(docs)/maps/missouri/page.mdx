import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Missouri Map',
  description: 'Interactive SVG map of Missouri with 115 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Missouri Map"
  description="Interactive SVG map of Missouri with 115 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="missouri"
>

<ExampleLoader
  filePath='app/(docs)/maps/missouri/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
