import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Spain Map',
  description: 'Interactive SVG map of Spain with 17 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Spain Map"
  description="Interactive SVG map of Spain with 17 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="spain"
>

<ExampleLoader
  filePath='app/(docs)/maps/spain/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
