import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Alaska Map',
  description: 'Interactive SVG map of Alaska with 29 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Alaska Map"
  description="Interactive SVG map of Alaska with 29 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="alaska"
>

<ExampleLoader
  filePath='app/(docs)/maps/alaska/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
