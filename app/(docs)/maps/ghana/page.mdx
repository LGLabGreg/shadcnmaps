import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Ghana Map',
  description: 'Interactive SVG map of Ghana with 10 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Ghana Map"
  description="Interactive SVG map of Ghana with 10 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="ghana"
>

<ExampleLoader
  filePath='app/(docs)/maps/ghana/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
