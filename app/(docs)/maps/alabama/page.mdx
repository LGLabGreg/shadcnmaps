import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Alabama Map',
  description: 'Interactive SVG map of Alabama with 67 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Alabama Map"
  description="Interactive SVG map of Alabama with 67 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="alabama"
>

<ExampleLoader
  filePath='app/(docs)/maps/alabama/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
