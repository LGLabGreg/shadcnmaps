import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'London Boroughs',
  description: 'Interactive SVG map of London Boroughs with 33 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="London Boroughs"
  description="Interactive SVG map of London Boroughs with 33 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="london-boroughs"
>

<ExampleLoader
  filePath='app/(docs)/maps/london-boroughs/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
