import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Oklahoma Map',
  description: 'Interactive SVG map of Oklahoma with 77 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Oklahoma Map"
  description="Interactive SVG map of Oklahoma with 77 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="oklahoma"
>

<ExampleLoader
  filePath='app/(docs)/maps/oklahoma/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
