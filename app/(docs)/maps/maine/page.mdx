import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Maine Map',
  description: 'Interactive SVG map of Maine with 16 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Maine Map"
  description="Interactive SVG map of Maine with 16 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="maine"
>

<ExampleLoader
  filePath='app/(docs)/maps/maine/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
