import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'New Hampshire Map',
  description: 'Interactive SVG map of New Hampshire with 10 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="New Hampshire Map"
  description="Interactive SVG map of New Hampshire with 10 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="new-hampshire"
>

<ExampleLoader
  filePath='app/(docs)/maps/new-hampshire/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
