import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Kuwait Map',
  description: 'Interactive SVG map of Kuwait with 6 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Kuwait Map"
  description="Interactive SVG map of Kuwait with 6 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="kuwait"
>

<ExampleLoader
  filePath='app/(docs)/maps/kuwait/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
