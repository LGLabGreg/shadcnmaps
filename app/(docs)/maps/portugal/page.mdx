import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Portugal Map',
  description: 'Interactive SVG map of Portugal with 20 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Portugal Map"
  description="Interactive SVG map of Portugal with 20 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="portugal"
>

<ExampleLoader
  filePath='app/(docs)/maps/portugal/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
