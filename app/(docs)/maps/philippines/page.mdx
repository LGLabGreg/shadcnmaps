import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Philippines Map',
  description: 'Interactive SVG map of Philippines with 119 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Philippines Map"
  description="Interactive SVG map of Philippines with 119 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="philippines"
>

<ExampleLoader
  filePath='app/(docs)/maps/philippines/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
