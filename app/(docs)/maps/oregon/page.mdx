import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Oregon Map',
  description: 'Interactive SVG map of Oregon with 36 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Oregon Map"
  description="Interactive SVG map of Oregon with 36 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="oregon"
>

<ExampleLoader
  filePath='app/(docs)/maps/oregon/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
