import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Florida',
  description: 'Interactive SVG map of Florida with 67 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Florida"
  description="Interactive SVG map of Florida with 67 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="florida"
>

<ExampleLoader
  filePath='app/(docs)/maps/florida/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
