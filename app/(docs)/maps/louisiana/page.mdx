import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Louisiana Map',
  description: 'Interactive SVG map of Louisiana with 64 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Louisiana Map"
  description="Interactive SVG map of Louisiana with 64 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="louisiana"
>

<ExampleLoader
  filePath='app/(docs)/maps/louisiana/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
