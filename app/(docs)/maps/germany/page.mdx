import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Germany Map',
  description: 'Interactive SVG map of Germany with 16 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Germany Map"
  description="Interactive SVG map of Germany with 16 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="germany"
>

<ExampleLoader
  filePath='app/(docs)/maps/germany/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
