import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Maryland Map',
  description: 'Interactive SVG map of Maryland with 24 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Maryland Map"
  description="Interactive SVG map of Maryland with 24 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="maryland"
>

<ExampleLoader
  filePath='app/(docs)/maps/maryland/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
