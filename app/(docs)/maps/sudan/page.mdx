import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Sudan Map',
  description: 'Interactive SVG map of Sudan with 17 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Sudan Map"
  description="Interactive SVG map of Sudan with 17 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="sudan"
>

<ExampleLoader
  filePath='app/(docs)/maps/sudan/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
