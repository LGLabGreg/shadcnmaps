import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Russia',
  description: 'Interactive SVG map of Russia with 85 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Russia"
  description="Interactive SVG map of Russia with 85 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="russia"
>

<ExampleLoader
  filePath='app/(docs)/maps/russia/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
