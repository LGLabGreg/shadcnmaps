import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Costa Rica Map',
  description: 'Interactive SVG map of Costa Rica with 7 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Costa Rica Map"
  description="Interactive SVG map of Costa Rica with 7 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="costa-rica"
>

<ExampleLoader
  filePath='app/(docs)/maps/costa-rica/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
