import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Iran Map',
  description: 'Interactive SVG map of Iran with 31 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Iran Map"
  description="Interactive SVG map of Iran with 31 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="iran"
>

<ExampleLoader
  filePath='app/(docs)/maps/iran/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
