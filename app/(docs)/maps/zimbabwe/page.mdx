import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Zimbabwe',
  description: 'Interactive SVG map of Zimbabwe with 10 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Zimbabwe"
  description="Interactive SVG map of Zimbabwe with 10 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="zimbabwe"
>

<ExampleLoader
  filePath='app/(docs)/maps/zimbabwe/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
