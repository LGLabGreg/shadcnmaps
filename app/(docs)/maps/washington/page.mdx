import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Washington Map',
  description: 'Interactive SVG map of Washington with 39 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Washington Map"
  description="Interactive SVG map of Washington with 39 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="washington"
>

<ExampleLoader
  filePath='app/(docs)/maps/washington/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
