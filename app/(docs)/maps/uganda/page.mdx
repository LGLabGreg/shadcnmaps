import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Uganda',
  description: 'Interactive SVG map of Uganda with 112 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Uganda"
  description="Interactive SVG map of Uganda with 112 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="uganda"
>

<ExampleLoader
  filePath='app/(docs)/maps/uganda/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
