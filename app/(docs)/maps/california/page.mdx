import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'California Map',
  description: 'Interactive SVG map of California with 58 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="California Map"
  description="Interactive SVG map of California with 58 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="california"
>

<ExampleLoader
  filePath='app/(docs)/maps/california/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
