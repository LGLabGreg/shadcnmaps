import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Myanmar Map',
  description: 'Interactive SVG map of Myanmar with 14 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Myanmar Map"
  description="Interactive SVG map of Myanmar with 14 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="myanmar"
>

<ExampleLoader
  filePath='app/(docs)/maps/myanmar/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
