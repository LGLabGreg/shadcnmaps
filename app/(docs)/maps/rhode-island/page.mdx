import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Rhode Island Map',
  description: 'Interactive SVG map of Rhode Island with 5 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Rhode Island Map"
  description="Interactive SVG map of Rhode Island with 5 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="rhode-island"
>

<ExampleLoader
  filePath='app/(docs)/maps/rhode-island/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
