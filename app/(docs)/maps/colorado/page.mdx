import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Colorado Map',
  description: 'Interactive SVG map of Colorado with 64 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Colorado Map"
  description="Interactive SVG map of Colorado with 64 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="colorado"
>

<ExampleLoader
  filePath='app/(docs)/maps/colorado/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
