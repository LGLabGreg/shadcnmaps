import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Vermont',
  description: 'Interactive SVG map of Vermont with 14 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Vermont"
  description="Interactive SVG map of Vermont with 14 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="vermont"
>

<ExampleLoader
  filePath='app/(docs)/maps/vermont/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
