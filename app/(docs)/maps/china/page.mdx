import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'China',
  description: 'Interactive SVG map of China with 32 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="China"
  description="Interactive SVG map of China with 32 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="china"
>

<ExampleLoader
  filePath='app/(docs)/maps/china/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
