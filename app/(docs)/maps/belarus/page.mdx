import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Belarus',
  description: 'Interactive SVG map of Belarus with 7 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Belarus"
  description="Interactive SVG map of Belarus with 7 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="belarus"
>

<ExampleLoader
  filePath='app/(docs)/maps/belarus/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
