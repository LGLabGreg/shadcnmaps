import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Venezuela',
  description: 'Interactive SVG map of Venezuela with 25 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Venezuela"
  description="Interactive SVG map of Venezuela with 25 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="venezuela"
>

<ExampleLoader
  filePath='app/(docs)/maps/venezuela/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
