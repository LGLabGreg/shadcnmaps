import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Ukraine Map',
  description: 'Interactive SVG map of Ukraine with 27 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Ukraine Map"
  description="Interactive SVG map of Ukraine with 27 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="ukraine"
>

<ExampleLoader
  filePath='app/(docs)/maps/ukraine/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
