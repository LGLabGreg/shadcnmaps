import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Tunisia Map',
  description: 'Interactive SVG map of Tunisia with 23 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Tunisia Map"
  description="Interactive SVG map of Tunisia with 23 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="tunisia"
>

<ExampleLoader
  filePath='app/(docs)/maps/tunisia/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
