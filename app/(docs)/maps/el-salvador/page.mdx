import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'El Salvador Map',
  description: 'Interactive SVG map of El Salvador with 14 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="El Salvador Map"
  description="Interactive SVG map of El Salvador with 14 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="el-salvador"
>

<ExampleLoader
  filePath='app/(docs)/maps/el-salvador/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
