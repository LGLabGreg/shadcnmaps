import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Peru Map',
  description: 'Interactive SVG map of Peru with 26 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Peru Map"
  description="Interactive SVG map of Peru with 26 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="peru"
>

<ExampleLoader
  filePath='app/(docs)/maps/peru/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
