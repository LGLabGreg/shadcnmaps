import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Kenya Map',
  description: 'Interactive SVG map of Kenya with 8 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Kenya Map"
  description="Interactive SVG map of Kenya with 8 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="kenya"
>

<ExampleLoader
  filePath='app/(docs)/maps/kenya/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
