import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Pakistan',
  description: 'Interactive SVG map of Pakistan with 8 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Pakistan"
  description="Interactive SVG map of Pakistan with 8 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="pakistan"
>

<ExampleLoader
  filePath='app/(docs)/maps/pakistan/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
