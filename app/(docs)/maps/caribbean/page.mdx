import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Caribbean Map',
  description: 'Interactive SVG map of Caribbean with 35 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Caribbean Map"
  description="Interactive SVG map of Caribbean with 35 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="caribbean"
>

<ExampleLoader
  filePath='app/(docs)/maps/caribbean/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
