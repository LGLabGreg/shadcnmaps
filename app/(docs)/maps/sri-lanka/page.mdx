import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Sri Lanka',
  description: 'Interactive SVG map of Sri Lanka with 25 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Sri Lanka"
  description="Interactive SVG map of Sri Lanka with 25 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="sri-lanka"
>

<ExampleLoader
  filePath='app/(docs)/maps/sri-lanka/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
