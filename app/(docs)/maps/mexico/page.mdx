import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Mexico Map',
  description: 'Interactive SVG map of Mexico with 32 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Mexico Map"
  description="Interactive SVG map of Mexico with 32 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="mexico"
>

<ExampleLoader
  filePath='app/(docs)/maps/mexico/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
