import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Bolivia',
  description: 'Interactive SVG map of Bolivia with 9 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Bolivia"
  description="Interactive SVG map of Bolivia with 9 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="bolivia"
>

<ExampleLoader
  filePath='app/(docs)/maps/bolivia/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
