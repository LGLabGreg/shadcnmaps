import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'European Union',
  description: 'Interactive SVG map of European Union with 28 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="European Union"
  description="Interactive SVG map of European Union with 28 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="european-union"
>

<ExampleLoader
  filePath='app/(docs)/maps/european-union/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
