import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Bulgaria',
  description: 'Interactive SVG map of Bulgaria with 28 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Bulgaria"
  description="Interactive SVG map of Bulgaria with 28 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="bulgaria"
>

<ExampleLoader
  filePath='app/(docs)/maps/bulgaria/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
