import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Continents Map',
  description: 'Interactive SVG map of Continents with 6 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Continents Map"
  description="Interactive SVG map of Continents with 6 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="continents"
>

<ExampleLoader
  filePath='app/(docs)/maps/continents/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
