import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Vietnam Map',
  description: 'Interactive SVG map of Vietnam with 63 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Vietnam Map"
  description="Interactive SVG map of Vietnam with 63 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="vietnam"
>

<ExampleLoader
  filePath='app/(docs)/maps/vietnam/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
