import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Honduras',
  description: 'Interactive SVG map of Honduras with 18 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Honduras"
  description="Interactive SVG map of Honduras with 18 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="honduras"
>

<ExampleLoader
  filePath='app/(docs)/maps/honduras/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
