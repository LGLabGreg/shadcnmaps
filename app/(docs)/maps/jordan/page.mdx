import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Jordan Map',
  description: 'Interactive SVG map of Jordan with 12 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Jordan Map"
  description="Interactive SVG map of Jordan with 12 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="jordan"
>

<ExampleLoader
  filePath='app/(docs)/maps/jordan/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
