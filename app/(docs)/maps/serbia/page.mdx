import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Serbia Map',
  description: 'Interactive SVG map of Serbia with 25 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Serbia Map"
  description="Interactive SVG map of Serbia with 25 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="serbia"
>

<ExampleLoader
  filePath='app/(docs)/maps/serbia/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
