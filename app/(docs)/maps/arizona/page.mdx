import { MapPage } from '@/components/docs/map-page'
import { ExampleLoader } from '@/components/docs/example-loader'

import SimpleExample from './map'

export const metadata = {
  title: 'Arizona Map',
  description: 'Interactive SVG map of Arizona with 15 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers.',
}

<MapPage
  title="Arizona Map"
  description="Interactive SVG map of Arizona with 15 regions. Supports click, hover, keyboard navigation, tooltips, and custom markers."
  registryName="arizona"
>

<ExampleLoader
  filePath='app/(docs)/maps/arizona/map.tsx'
>
  <SimpleExample />
</ExampleLoader>

</MapPage>
